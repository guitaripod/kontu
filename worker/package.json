{
  "name": "kontu-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply kontu --local",
    "db:migrate:remote": "wrangler d1 migrations apply kontu --remote"
  },
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260623.1",
    "node-html-parser": "^8.0.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.104.0"
  }
}
